[permissions]
default_profile = "edit"

[gates]
required = ["tests", "build", "rust", "audit"]

[gates.commands]
tests = "npm run test:release:app"
build = "npm run build"
rust = "cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml"
audit = "npm run audit:check"
